@page "/feature-flags"
@inject NavigationManager NavigationManager

<PageTitle>Settings — Data Explorer</PageTitle>

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/settings/feature-flags", replace: true);
    }
}
